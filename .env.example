# Stripe TEST MODE only (sk_test_… / pk_test_…). Never commit real secrets.
# Checkout rejects production keys — use test mode only.
STRIPE_SECRET_KEY=sk_test_replace_me
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me

# Founding annual price (default used if unset)
STRIPE_PRICE_FOUNDING_ANNUAL=price_1UEZusGTDaLsaWdvvifY0LxN
